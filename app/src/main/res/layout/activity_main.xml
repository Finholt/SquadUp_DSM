<!--The following activity was used for testing purposes regarding navigation in the
earlier stages of development.  We should have deleted it before finishing, but if
you're reading this, clearly we forgot.  Oops.-->

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/LoginButton"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:onClick="loginProcedure"
        android:text="Login"
        android:textAllCaps="false" />

    <Button
        android:id="@+id/SignUpButton"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@+id/LoginButton"
        android:onClick="signUpProcedure"
        android:text="Signup"
        android:textAllCaps="false" />

    <Button
        android:id="@+id/OtherButton"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@+id/SignUpButton"
        android:onClick="otherLogin"
        android:text="Other"
        android:textAllCaps="false" />

</RelativeLayout>
